{
  "name": "zoom-icebreaker-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Front-end test harness for the Icebreaker web UI (app.js / roster.js).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "record:demo": "node scripts/record-demo.mjs"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^4.1.10",
    "jsdom": "^29.1.1",
    "playwright": "1.61.1",
    "vitest": "^4.1.10"
  },
  "//overrides": "Exact pins (not ^) so a caret can't pull in a release too new to have been vetted. postcss remediates GHSA-fxqj-rqcc-2cmp (and the earlier GHSA-r28c-9q8g-f849); nanoid remediates GHSA-28wg-ghj8-5hjv / GHSA-2v37-7h3g-55p8; the other two are transitives that a lockfile refresh floated to same-day publishes.",
  "overrides": {
    "postcss": "8.5.24",
    "nanoid": "3.3.18",
    "@napi-rs/wasm-runtime": "1.1.6",
    "tinyrainbow": "3.1.0"
  }
}
